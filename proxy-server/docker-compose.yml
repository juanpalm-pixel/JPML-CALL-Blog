version: '3.8'

services:
  proxy:
    build: .
    container_name: abair-stt-proxy
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - abair-network

networks:
  abair-network:
    driver: bridge
